---
title: 'Migrating to 1.x.x'
---

import { ReactJsData } from '@components/assets/Data/data';
import GradientCard from '@components/common/GradientCard/GradientCard';
import { Callout } from 'nextra-theme-docs';

# Migrating to 1.x.x

<Callout>
  These are the <b>beta</b> version of the SDKs. We are still working on them. If you have any feedback, please share it on our discord channel. For the previous latest stable release, please visit <a className="text-bold underline" href="https://legacy.docs.huddle01.com/docs">here</a>
</Callout>

The Huddle01 React SDK offers a comprehensive suite of hooks, methods and event listeners that allow for seamless real-time audio and video communication with minimal coding required.

<div className="my-16 xl:max-w-none">
  <div className="not-prose mt-4 grid grid-cols-1 gap-8 sm:grid-cols-2 xl:grid-cols-3">
    {ReactJsData.map(resource => (
      <GradientCard key={resource.href} resource={resource} />
    ))}
  </div>
</div>

## Pre Requisites

Before using the Huddle01 React SDK, you must have Node.js installed on your machine. You can install Node.js using one of the following methods:

- Install Node.js with NVM by following the instructions on this **[GitHub page](https://github.com/nvm-sh/nvm)**
- Install Node.js with FNM by following the instructions on this **[GitHub page](https://github.com/Schniz/fnm)**
- Download Node.js from the official **[website](https://nodejs.org/)**

<Callout>
 Pro Tip: Use NVM or FNM to install Node.js as they help in version management and easy updation

</Callout>

After installing Node.js, verify it's installed by running the following command in your terminal:

```jsx
node --version
```
