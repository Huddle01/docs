---
title: 'Showcase'
---


{/* Data */}
import { showcaseData } from '@components/assets/Data/data';

{/* Components */}
import ShowcaseCard from '@components/Card/ShowcaseCard.tsx';
import LatestRelease, {getLatestReleaseData} from "@components/revamp/LatestRelease/LatestRelease.tsx";

export const getStaticProps = getLatestReleaseData;

<div className="grid md:grid-cols-[2.5fr,1fr] grid-cols-1 w-full h-full ">

{/* Left Section */}
<div className="flex flex-col h-full md:border-x border-rgb-1 md:p-9 w-full">
<div className="flex md:items-center w-full md:justify-between md:flex-row flex-col">
# Showcase
<div className="text-rgb-10 text-xs font-normal mt-1 md:mt-0">Last updated on June 16, 2023</div>
</div>

<div className="text-[#9EA3AE] text-base font-normal md:mt-0 mt-4 mb-10">Popular apps and projects built with Huddle01 across the world.</div>
{/* Projects Cards */}
<div className="grid md:grid-cols-3 grid-cols-1 gap-4">
{showcaseData.map((item) => (
  <ShowcaseCard key={item.name} name={item.name} href={item.href} desc={item.description}/>
))}
</div>
</div>


{/* Right Section */}   
<LatestRelease/>
</div>
