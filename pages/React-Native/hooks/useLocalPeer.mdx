---
title: 'useLocalPeer'
---

import OptionTable from '@components/common/Table/OptionTable';
import Pill from '@components/common/Pill/Pill';

# useLocalPeer

The useLocalPeer hook allows you to interact with your own Peer object, which is how you are represented inside a Huddle01 room.


<OptionTable
  headers={['Name', 'Description', 'Return Type', 'Params']}
  options={[
    [
      'peerId',
      'Your peerId.',
      'string | null',
      ''
    ],
    [
      'metadata',
      'The metadata associated with you.',
      'unknown',
      ''
    ],
    [
      'permissions',
      'Your peer permissions inside the room.',
      'TPermissions',
      ''
    ],
    [
      'updateMetadata',
      'Update the metadata associated with your peer object.',
      'void',
      'data: unknown'
    ],
    [
      'role',
      'Your peer role inside the room.',
      'string',
      ''
    ],
    [
      'updateRole',
      'Update the role associated with your peer object.',
      'void',
      'role: string'
    ],
  ]}
/>


```tsx
const {
    peerId,
    role,
    permissions,
    metadata,
    updateMetadata,
    updateRole,
  } = useLocalPeer<{name: string}>({
    onMetadataUpdated(metadata) {},
    onPermissionsUpdated(permissions) {},
    onRoleUpdated(role) {},
  });

// update your metadata
const updateMetadata = () => {
  updateMetadata({ name: 'John Doe' });
};

// update your role
const updateRole = () => {
  updateRole('coHost');
};
```

## Props
The useLocalPeer hook accepts an object with the following fields as props.

{/* onMetadataUpdated */}
### <span className="font-mono font-semibold">onMetadataUpdated</span>
<div className='flex w-40 justify-between mt-4'>
  <Pill>Optional</Pill>
  <Pill>Advanced</Pill>
</div>

<OptionTable
  headers={['Description', 'Return Type']}
  options={[
    [
      'This function will be called when the metadata associated with you is updated.',
      'void',
    ],
  ]}
/>

<OptionTable
  headers={['Parameter Name', 'Type', 'Description', 'Required']}
  options={[
    [
      'metadata',
      'unknown',
      'The metadata associated with your peer object.',
      'Yes',
    ],
  ]}
/>

#### Example Usage

```tsx
const localPeer = useLocalPeer({ onMetadataUpdated: (metadata) => {
	console.log("Your metadata was updated!");
	console.log(metadata);
    // your code here
}});
```

{/* onPermissionsUpdated */}
### <span className="font-mono font-semibold">onPermissionsUpdated</span>
<div className='flex w-40 justify-between mt-4'>
  <Pill>Optional</Pill>
  <Pill>Advanced</Pill>
</div>

<OptionTable
  headers={['Description', 'Return Type']}
  options={[
    [
      'This function will be called when your user permissions are updated.',
      'void',
    ],
  ]}
/>

<OptionTable
  headers={['Parameter Name', 'Type', 'Description', 'Required']}
  options={[
    [
      'permissions',
      'TPermissions',
      'The permissions associated with your peer object.',
      'Yes',
    ],
  ]}
/>

#### Example Usage

```tsx
const localPeer = useLocalPeer({ onPermissionsUpdated: (permissions) => {
	console.log("Your permissions were updated!");
	console.log(permissions);
    // your code here
}});
```

{/* onRoleUpdated */}
### <span className="font-mono font-semibold">onRoleUpdated</span>
<div className='flex w-40 justify-between mt-4'>
  <Pill>Optional</Pill>
  <Pill>Advanced</Pill>
</div>

<OptionTable
  headers={['Description', 'Return Type']}
  options={[
    [
      'This function will be called when your user role is updated.',
      'void',
    ],
  ]}
/>

<OptionTable
  headers={['Parameter Name', 'Type', 'Description', 'Required']}
  options={[
    [
      'role',
      'string',
      'The role associated with your peer object.',
      'Yes',
    ],
  ]}
/>

#### Example Usage

```tsx
const localPeer = useLocalPeer({ onRoleUpdated: (role) => {
	console.log("Your role was updated!");
	console.log(role);
    // your code here
}});
```