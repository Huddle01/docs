---
title: 'Home'
---

import Image from "next/image";

{/* Assets */}
import LandingIcons from '@components/assets/LandingIcons';

{/* Data */}
import iFrameApiData from "Data/iFrame&Api.json";
import GetStartedData from "Data/GetStarted.json";


{/* Components */}
import CustomBtn from "@components/revamp/CustomBtn/CustomBtn.tsx"
import SubCard from "@components/revamp/LandingCards/SubCard.tsx"
import LatestRelease, {getLatestReleaseData} from "@components/revamp/LatestRelease/LatestRelease.tsx"
import LandingCards from "@components/revamp/LandingCards/LandingCards.tsx"
import {GetStartedCards,CLIToolCard,LinksIcon} from "@components/revamp/CommonCards/CommonCards.tsx"
import { Callout } from 'nextra-theme-docs';

export const getStaticProps = getLatestReleaseData;

<div className="grid lg:grid-cols-[2.5fr,1fr] grid-cols-1 w-full min-h-full">

<div className="flex flex-col h-full md:border-x border-rgb-1 md:p-9">


# Welcome to Huddle01 Builders’ Kit

<Callout>
  These are the <b>beta</b> version of the SDKs. We are still working on them. If you have any feedback, please share it on our discord channel. For the previous latest stable release, please visit <a className="text-bold underline" href="https://legacy.docs.huddle01.com/docs">here</a>
</Callout>

From Guides to SDKs to API references - find everything you need to build high-quality live audio and video experiences at one place. Scalable and customizable - our SDKs are built for both web and mobile applications.


{/* Popular Guides Section */}
<LandingCards title="Popular guides" type="Guide" className="flex-col mt-10"> 
   <div className="grid lg:grid-cols-3 gap-4 grid-cols-1 mt-6">
   {[{title:"Audio Spaces",url:"/guides/audio-spaces"},{title:"Video Meeting",url:"/React/react-walkthrough"},{title:"Token-gated Room",url:"/guides/token-gated"}].map(({title,url}) => (
    <SubCard key={title} title={title} url={url}/>
   ))}                                                                                                                                                                                                                                                                           
   </div>
</LandingCards> 


{/* API Key Card Section */}
<LandingCards title="Get your API Key" type="apiKey" className="flex-row items-center justify-between cursor-pointer mb-6 mt-10 hover:bg-[#101114] duration-300 ease-in-out" url="/api-keys">
<div>{LandingIcons["chevron-right"]}</div>
</LandingCards>


{/*Get Started Cards Section */}
<LandingCards className="mt-6" type="start" title="Get started quickly with SDKs" className="flex-col">
<div className="grid lg:grid-cols-3 grid-cols-1 w-full mt-7 mb-6 gap-4">
{GetStartedData.map(({title ,url}) => (
<GetStartedCards key={title} title={title} url={url}/>
))}
</div>
<CLIToolCard/>
</LandingCards>


{/* IFrame & API Ref. Section */}
<div className="grid lg:grid-cols-2 grid-cols-1 md:gap-4 mt-6">
{iFrameApiData.map(({title,type,desc,linksArr,url}) => (
<LandingCards title={title} type={type} className="flex-col" key={title} url={url} className="flex flex-col cursor-pointer ease-in-out duration-300">
<p className="text-base font-normal text-custom-4 my-4">{desc}</p>
<div className="flex items-center gap-4">
{linksArr.map(({title,linkUrl}) => (
<LinksIcon title={title} linkUrl={linkUrl} key={title}/>
))}
</div>
</LandingCards>
))}
</div>


{/* Get Started Section */}
<div className="border-t border-custom-1 mt-10 pt-4 flex items-center justify-between">
<div className="text-xs font-normal text-rgb-8">Last updated on June 16, 2023</div>

<CustomBtn className="gap-2 border border-custom-5 rounded-lg py-2 px-4" url="/get-started">
<div className="text-sm font-semibold text-rgb-7">Get Started</div>
<div>{LandingIcons["chevron-right-btn"]}</div>
</CustomBtn>
</div>
</div>

<LatestRelease />
</div>
