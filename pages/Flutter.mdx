---
title: 'Flutter'
---

import { FlutterSDKData } from '@components/assets/Data/data';
import GradientCard from '@components/common/GradientCard/GradientCard';
import { Callout } from 'nextra-theme-docs';

# Flutter SDK

The Huddle01 Flutter SDK offers a comprehensive suite of hooks, methods and event listeners that allow for seamless real-time audio and video communication with minimal coding required.

<div className="my-16 xl:max-w-none">
  <div className="not-prose mt-4 grid grid-cols-1 gap-8 sm:grid-cols-2 xl:grid-cols-3">
    {FlutterSDKData.map(resource => (
      <GradientCard key={resource.href} resource={resource} />
    ))}
  </div>
</div>

