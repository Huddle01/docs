---
title: 'useRemotePeer'
---

import OptionTable from '@components/common/Table/OptionTable';
import Pill from '@components/common/Pill/Pill';

# useRemotePeer

The useRemotePeer hook allows you to interact with another user's Peer object, which is how they are represented inside a Huddle01 room.

```tsx
const {
    peerId,
    role,
    metadata,
    updateRole,
  } = useRemotePeer({
    peerId: "remote-peer-id",
    onMetadataUpdate(data) {},
  });
```

## Props
The useRemotePeer hook accepts an object with the following fields as props.

{/* peerId */}
### <span className="font-mono font-semibold">peerId</span>
<div className='w-20 mt-4'>
  <Pill>Required</Pill>
</div>

<OptionTable
  headers={['Description', 'Type']}
  options={[
    [
      "The peerId of the remote peer.",
      'string',
    ],
  ]}
/>


#### Example Usage

```tsx
const remotePeer = useRemotePeer({ peerId: "remote-peer-id" });
```

{/* onMetadataUpdated */}
### <span className="font-mono font-semibold">onMetadataUpdated</span>
<div className='flex w-40 justify-between mt-4'>
  <Pill>Optional</Pill>
  <Pill>Advanced</Pill>
</div>

<OptionTable
  headers={['Description', 'Return Type']}
  options={[
    [
      'This function will be called when the metadata associated with the remote peer is updated.',
      'void',
    ],
  ]}
/>

<OptionTable
  headers={['Parameter Name', 'Type', 'Description', 'Required']}
  options={[
    [
      'data',
      '{ metadata: unknown }',
      'An object containing the metadata associated with the remote peer object.',
      'Yes',
    ],
  ]}
/>

#### Example Usage

```tsx
const remotePeer = useRemotePeer({ peerId: "remote-peer-id", onMetadataUpdated: (data) => {
	console.log("Remote peer's metadata was updated!");
	console.log(data.metadata);
    // your code here
}});
```


## Returns
The useLocalPeer hook returns an object with the following fields.

{/* peerId */}
### <span className="font-mono font-semibold">peerId</span>
<div className='w-20 mt-4'>
  <Pill>Object</Pill>
</div>

<OptionTable
  headers={['Description', 'Type']}
  options={[
    [
      "The remote peer's peerId.",
      'string | undefined',
    ],
  ]}
/>

{/* metadata */}
### <span className="font-mono font-semibold">metadata</span>
<div className='flex w-40 justify-between mt-4'>
  <Pill>Object</Pill>
  <Pill>Advanced</Pill>
</div>

<OptionTable
  headers={['Description', 'Type']}
  options={[
    [
      'The metadata associated with the remote peer.',
      'unknown',
    ],
  ]}
/>

{/* role */}
### <span className="font-mono font-semibold">role</span>
<div className='w-20 mt-4'>
  <Pill>Object</Pill>
</div>

<OptionTable
  headers={['Description', 'Type']}
  options={[
    [
      'Your peer role.',
      'string',
    ],
  ]}
/>



{/* updateRole */}
### <span className="font-mono font-semibold">updateRole</span>
<div className='w-20 mt-4'>
  <Pill>Function</Pill>
</div>

<OptionTable
  headers={['Description', 'Return Type']}
  options={[
    [
      'Update the metadata associated with your peer object.',
      'void',
    ],
  ]}
/>

<OptionTable
  headers={['Parameter Name', 'Type', 'Description', 'Required']}
  options={[
    [
      'data',
      'CO-HOST,LISTENER,GUEST',
      'The role you want to be associated with your peer object.',
      'Yes',
    ],
  ]}
/>
