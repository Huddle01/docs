---
title: 'useRoomMetadata'
---

import OptionTable from '@components/common/Table/OptionTable';
import Pill from '@components/common/Pill/Pill';

# useRoomMetadata

The  `useRoomMetadata`  hook returns the metadata associated with the room and the function to update it.

<OptionTable
  headers={['Name', 'Description', 'Return Type', 'Params']}
  options={[
    [
      'metadata',
      'The room metadata',
      'unknown',
      ''
    ],
    [
      'updateMetadata',
      'Updates the room metadata',
      'void',
      'metadata: unknown'
    ]
  ]}
/>

#### Example Usage

```tsx
const {
    roomData,
    updateMetadata,
  } = useRoomMetadata<{title: string}>({
    onMetadataUpdated(metadata:unknown) {},
    onLeave(data: {
            reason: 'LEFT' | 'CLOSED' | 'KICKED' | 'DENIED';
            message?: string;
    }) {}
  });

const updateMetadata = () => {
    updateMetadata({
        string: "Huddle01 Room"
    });
}
```

## Props
The useRoomMetadata hook accepts an object with the following fields as props.

{/* onMetadataUpdated */}
### <span className="font-mono font-semibold">onMetadataUpdated</span>
<div className='w-20 mt-4'>
  <Pill>Optional</Pill>
</div>

<OptionTable
  headers={['Description', 'Type']}
  options={[
    [
      "This function will be called when the room metadata gets updated.",
      "void",
    ],
  ]}
/>

#### Example Usage

```tsx
const devices = useRoomMetadata({ onMetadataUpdated:() => {
    console.log("Triggered when metadata updated")
}});
```


{/* onLeave */}
### <span className="font-mono font-semibold">onLeave</span>
<div className='w-20 mt-4'>
  <Pill>Optional</Pill>
</div>

<OptionTable
  headers={['Description', 'Type']}
  options={[
    [
      "This function will be called when the peer leaves.",
      "void",
    ],
  ]}
/>

#### Example Usage

```tsx
const devices = useRoomMetadata({ onLeave:() => {
    console.log("Triggered when peer leaves")
}});
```


