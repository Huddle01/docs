---
title: 'useRoomControls'
---

import OptionTable from '@components/common/Table/OptionTable';
import Pill from '@components/common/Pill/Pill';

# useRoomControls

The useRoomControls hook allows you to read and update the room's controls.

```tsx
const {
  roomControls,
  updateControls,
  } = useRoomControls({
  onRoomControlsUpdated(data: NewRoomControls) ();
  onRoomLeave(data: RoomEvents['room-closed'][0]) ();
  });
```


## Props
The useRoomControls hook accepts an object with the following fields as props.

{/* onRoomControlsUpdated */}
<div className="flex mt-5">
    <span className="font-mono font-semibold">1. onRoomControlsUpdated</span>
    <span className="ml-2">
        <Pill>Optional</Pill>
    </span>
    <span className="ml-2">
        <Pill>Advanced</Pill>
    </span>
</div>

<OptionTable
  headers={['Description', 'Return Type']}
  options={[
    [
      'This function will be called when the room controls have been updated.',
      'void',
    ],
  ]}
/>

<OptionTable
  headers={['Parameter Name', 'Type', 'Description', 'Required']}
  options={[
    [
      'data',
      'NewRoomControls',
      'Object containing the new room controls.',
      'Yes',
    ],
  ]}
/>

##### Example Usage

```tsx
const roomControls = useRoomControls({ onRoomControlsUpdated: (data) => {
	console.log("Room controls updated!");
	console.log(data);
    // your code here
}});
```

{/* onRoomLeave */}
<div className="flex mt-5">
    <span className="font-mono font-semibold">2. onRoomLeave</span>
    <span className="ml-2">
        <Pill>Optional</Pill>
    </span>
    <span className="ml-2">
        <Pill>Advanced</Pill>
    </span>
</div>

<OptionTable
  headers={['Description', 'Return Type']}
  options={[
    [
      'This room controls will be updated when the peer leaves the room',
      'void',
    ],
  ]}
/>

<OptionTable
  headers={['Parameter Name', 'Type', 'Description', 'Required']}
  options={[
    [
      'data',
      'RoomEvents["room-closed"][0]',
      'Object containing the new room controls.',
      'Yes',
    ],
  ]}
/>

##### Example Usage

```tsx
const roomControls = useRoomControls({ onRoomLeave: (data) => {
	console.log("Room controls updated!");
	console.log(data);
    // your code here
}});
```

## Returns
The useRoomControls hook returns an object with the following fields.

{/* roomControls */}
<div className="flex mt-5">
    <span className="font-mono font-semibold">1. roomControls</span>
    <span className="ml-2">
        <Pill>Object</Pill>
    </span>
    <span className="ml-2">
        <Pill>Advanced</Pill>
    </span>
</div>

<OptionTable
  headers={['Description', 'Type']}
  options={[
    [
      'The state of the currently joined room.',
      'TRoomControls',
    ],
  ]}
/>

{/* updateControls */}
<div className="flex mt-5">
    <span className="font-mono font-semibold">2. updateControls</span>
    <span className="ml-2">
        <Pill>Function</Pill>
    </span>
    <span className="ml-2">
        <Pill>Advanced</Pill>
    </span>
</div>

<OptionTable
  headers={['Description', 'Return Type']}
  options={[
    [
      'Update the room control.',
      'void',
    ],
  ]}
/>

<OptionTable
  headers={['Parameter Name', 'Type', 'Description', 'Required']}
  options={[
    [
      'data',
      'TNewRoomControls',
      'Object containing the type of room control to be updated and it’s new value.',
      'Yes',
    ],
  ]}
/>
