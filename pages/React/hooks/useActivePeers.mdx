---
title: 'useActivePeers'
---


import OptionTable from '@components/common/Table/OptionTable';
import Pill from '@components/common/Pill/Pill';

# useActivePeers

The  ` useActivePeers `  hook returns the last most active n number of peers i.e. `activePeerIds` & `dominantSpeakerId`.


```tsx
const {
    activePeerIds,
    dominantSpeakerId,
  } = useActivePeers();
```

## Returns
The useActivePeers hook returns an object with the following fields.


{/* activePeerIds */}
<div className="flex mt-5">
    <span className="font-mono font-semibold">1. activePeerIds</span>
    <span className="ml-2">
        <Pill>object</Pill>
    </span>
</div>

<OptionTable
  headers={['Description', 'Type']}
  options={[
    [
      'last most active n number of peers.',
      'string[]',
    ],
  ]}
/>



{/* dominantSpeakerId */}
<div className="flex mt-5">
    <span className="font-mono font-semibold">2. dominantSpeakerId</span>
    <span className="ml-2">
        <Pill>object</Pill>
    </span>
</div>

<OptionTable
  headers={['Description', 'Type']}
  options={[
    [
      'last most active peer.',
      'string',
    ],
  ]}
/>