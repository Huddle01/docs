---
title: 'usePeerIds'
---

import OptionTable from '@components/common/Table/OptionTable';
import Pill from '@components/common/Pill/Pill';

# usePeerIds

The usePeerIds hook returns peerIds of all peers inside a Huddle01 room.

```tsx
const {
    peerIds
  } = usePeerIds({
    labels: ["audio"]
  });
```

## Props
The usePeerIds hook accepts an object with the following fields as props.

{/* labels */}
<div className="flex mt-5">
    <span className="font-mono font-semibold">1. labels</span>
    <span className="ml-2">
        <Pill>Optional</Pill>
    </span>
</div>

<OptionTable
  headers={['Description', 'Type']}
  options={[
    [
      'TODO: NOT SURE WHAT THIS IS FOR.',
      "Array<'audio' | 'video' | 'screen-video' | 'screen-audio' | string>",
    ],
  ]}
/>


##### Example Usage

```tsx
const peerIds = usePeerIds({ labels: ["audio"] });
```

## Returns
The usePeerIds hook returns an object with the following fields.

{/* peerIds */}
<div className="flex mt-5">
    <span className="font-mono font-semibold">1. peerIds</span>
    <span>
        <Pill>Array</Pill>
    </span>
</div>

<OptionTable
  headers={['Description', 'Type']}
  options={[
    [
      'List of peerIds.',
      'string[]',
    ],
  ]}
/>
