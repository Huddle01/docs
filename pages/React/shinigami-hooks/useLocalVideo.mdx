---
title: 'useLocalVideo'
---

import OptionTable from '@components/common/Table/OptionTable';
import Pill from '@components/common/Pill/Pill';

# useLocalVideo

The useLocalVideo hook exposes primitives to interact with your own video stream - coming from your camera device.

```tsx
const {
    stream,
    track,
    isVideoOn,
    enableVideo,
    disableVideo,
    replaceVideoStream,
    changeVideoSource,
  } = useLocalVideo({
    onProduceStart(producer) {},
    onProduceClose(reason :{
      code:number,
      tag:string,
      message:string
    }) {},
  });
```

## Props
The useLocalVideo hook accepts an object with the following fields as props.

{/* onProduceStart */}
<div className="flex mt-5">
    <span className="font-mono font-semibold">1. onProduceStart</span>
    <span className="ml-2">
        <Pill>Optional</Pill>
    </span>
    <span className="ml-2">
        <Pill>Advanced</Pill>
    </span>
</div>

<OptionTable
  headers={['Description', 'Return Type']}
  options={[
    [
      "This function will be called when you start producing your camera's video stream i.e sharing it with other peers in the room.",
      'void',
    ],
  ]}
/>

<OptionTable
  headers={['Parameter Name', 'Type', 'Description', 'Required']}
  options={[
    [
      'producer',
      'Producer',
      'The mediasoup producer object.',
      'Yes',
    ],
  ]}
/>

##### Example Usage

```tsx
const localVideo = useLocalVideo({ onProduceStart: (producer) => {
	console.log("Started producing your video stream!");
	console.log(producer);
    // your code here
}});
```

{/* onProduceClose */}
<div className="flex mt-5">
    <span className="font-mono font-semibold">2. onProduceClose</span>
    <span className="ml-2">
        <Pill>Optional</Pill>
    </span>
    <span className="ml-2">
        <Pill>Advanced</Pill>
    </span>
</div>

<OptionTable
  headers={['Description', 'Return Type']}
  options={[
    [
      "This function will be called when you stop producing your camera's video stream.",
      'void',
    ],
  ]}
/>

##### Example Usage

```tsx
const localVideo = useLocalVideo({ onProduceClose: (reason) => {
	console.log("Stopped producing your video stream!");
    // your code here
}});
```



## Returns
The useLocalVideo hook returns an object with the following fields.

{/* stream */}
<div className="flex mt-5">
    <span className="font-mono font-semibold">1. stream</span>
    <span className="ml-2">
        <Pill>Object</Pill>
    </span>
</div>

<OptionTable
  headers={['Description', 'Type']}
  options={[
    [
      "Your camera's video stream. null if not enabled yet.",
      'MediaStream | null',
    ],
  ]}
/>

{/* track */}
<div className="flex mt-5">
    <span className="font-mono font-semibold">2. track</span>
    <span className="ml-2">
        <Pill>Object</Pill>
    </span>
</div>

<OptionTable
  headers={['Description', 'Type']}
  options={[
    [
      "Your camera's video stream track. null if not enabled yet.",
      'MediaStreamTrack | null',
    ],
  ]}
/>

{/* isVideoOn */}
<div className="flex mt-5">
    <span className="font-mono font-semibold">3. isVideoOn</span>
    <span className="ml-2">
        <Pill>Object</Pill>
    </span>
</div>

<OptionTable
  headers={['Description', 'Type']}
  options={[
    [
      "Your camera's video stream is on or off.",
      'boolean',
    ],
  ]}
/>

{/* enableVideo */}
<div className="flex mt-5">
    <span className="font-mono font-semibold">4. enableVideo</span>
    <span className="ml-2">
        <Pill>Function</Pill>
    </span>
</div>

<OptionTable
  headers={['Description', 'Return Type']}
  options={[
    [
      "Enable your camera's video stream and start producing it with other peers in the room.",
      'Promise<void>',
    ],
  ]}
/>

{/* disableVideo */}
<div className="flex mt-5">
    <span className="font-mono font-semibold">5. disableVideo</span>
    <span className="ml-2">
        <Pill>Function</Pill>
    </span>
</div>

<OptionTable
  headers={['Description', 'Return Type']}
  options={[
    [
      "Disable your camera's video stream and stop producing it with other peers in the room.",
      'Promise<void>',
    ],
  ]}
/>

{/* replaceVideoStream */}
<div className="flex mt-5">
    <span className="font-mono font-semibold">6. replaceVideoStream</span>
    <span className="ml-2">
        <Pill>Function</Pill>
    </span>
</div>

<OptionTable
  headers={['Description', 'Return Type']}
  options={[
    [
      "Replaces the  mediastream.",
      'Promise<void>',
    ],
  ]}
/>


{/* changeVideoSource */}
<div className="flex mt-5">
    <span className="font-mono font-semibold">7. changeVideoSource</span>
    <span className="ml-2">
        <Pill>Function</Pill>
    </span>
</div>

<OptionTable
  headers={['Description', 'Return Type']}
  options={[
    [
      "Changes the video sources of the devices.",
      'Promise<void>',
    ],
  ]}
/>



