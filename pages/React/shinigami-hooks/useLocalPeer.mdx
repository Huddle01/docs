---
title: 'useLocalPeer'
---

import OptionTable from '@components/common/Table/OptionTable';
import Pill from '@components/common/Pill/Pill';

# useLocalPeer

The useLocalPeer hook allows you to interact with your own Peer object, which is how you are represented inside a Huddle01 room.

```tsx
const {
    peerId,
    role,
    permissions,
    metadata,
    updateMetadata,
    updateRole,
  } = useLocalPeer({
    onMetadataUpdated(metadata) {},
  });
```

## Props
The useLocalPeer hook accepts an object with the following fields as props.

{/* onMetadataUpdated */}
<div className="flex mt-5">
    <span className="font-mono font-semibold">1. onMetadataUpdated</span>
    <span className="ml-2">
        <Pill>Optional</Pill>
    </span>
    <span className="ml-2">
        <Pill>Advanced</Pill>
    </span>
</div>

<OptionTable
  headers={['Description', 'Return Type']}
  options={[
    [
      'This function will be called when the metadata associated with you is updated.',
      'void',
    ],
  ]}
/>

<OptionTable
  headers={['Parameter Name', 'Type', 'Description', 'Required']}
  options={[
    [
      'metadata',
      'unknown',
      'The metadata associated with your peer object.',
      'Yes',
    ],
  ]}
/>

##### Example Usage

```tsx
const localPeer = useLocalPeer({ onMetadataUpdated: (metadata) => {
	console.log("Your metadata was updated!");
	console.log(metadata);
    // your code here
}});
```

## Returns
The useLocalPeer hook returns an object with the following fields.

{/* peerId */}
<div className="flex mt-5">
    <span className="font-mono font-semibold">1. peerId</span>
    <span className="ml-2">
        <Pill>Object</Pill>
    </span>
</div>

<OptionTable
  headers={['Description', 'Type']}
  options={[
    [
      'Your peerId.',
      'string | null',
    ],
  ]}
/>

{/* metadata */}
<div className="flex mt-5">
    <span className="font-mono font-semibold">2. metadata</span>
    <span className="ml-2">
        <Pill>Object</Pill>
    </span>
    <span className="ml-2">
        <Pill>Advanced</Pill>
    </span>
</div>

<OptionTable
  headers={['Description', 'Type']}
  options={[
    [
      'The metadata associated with you.',
      'unknown',
    ],
  ]}
/>

{/* permissions */}
<div className="flex mt-5">
    <span className="font-mono font-semibold">3. permissions</span>
    <span className="ml-2">
        <Pill>Object</Pill>
    </span>
</div>

<OptionTable
  headers={['Description', 'Type']}
  options={[
    [
      'Your peer permissions inside the room.',
      'TPermissions',
    ],
  ]}
/>


{/* updateMetadata */}
<div className="flex mt-5">
    <span className="font-mono font-semibold">4. updateMetadata</span>
    <span className="ml-2">
        <Pill>Function</Pill>
    </span>
</div>

<OptionTable
  headers={['Description', 'Return Type']}
  options={[
    [
      'Update the metadata associated with your peer object.',
      'void',
    ],
  ]}
/>

<OptionTable
  headers={['Parameter Name', 'Type', 'Description', 'Required']}
  options={[
    [
      'data',
      'unknown',
      'The metadata you want to be associated with your peer object.',
      'Yes',
    ],
  ]}
/>

{/* role */}
<div className="flex mt-5">
    <span className="font-mono font-semibold">5. role</span>
    <span className="ml-2">
        <Pill>state</Pill>
    </span>
</div>

<OptionTable
  headers={['Description', 'Type']}
  options={[
    [
      'Update the metadata associated with your peer object.',
      'string',
    ],
  ]}
/>



{/* updateRole */}
<div className="flex mt-5">
    <span className="font-mono font-semibold">6. updateRole</span>
    <span className="ml-2">
        <Pill>function</Pill>
    </span>
</div>

<OptionTable
  headers={['Description', 'Return Type']}
  options={[
    [
      'Update the metadata associated with your peer object.',
      'void',
    ],
  ]}
/>

<OptionTable
  headers={['Parameter Name', 'Type', 'Description', 'Required']}
  options={[
    [
      'data',
      'CO-HOST,LISTENER,GUEST',
      'The role you want to be associated with your peer object.',
      'Yes',
    ],
  ]}
/>


