---
title: "Send Message"
---


import OptionTable from "@components/common/Table/OptionTable";
import { Callout } from "nextra-theme-docs";

### SendData(string message)

Call this function to send any string data or message to all peers in the room. All the peers which are present in the room will receive the message

**Parameters**

<OptionTable
  headers={["Params", "Type", "Description"]}
  options={[
    ["data", "string", "Any data or message in form of string"]
    ]}
/>

```csharp filename=".csharp"
public void SendDataToAllPeersInRoom(string data)
{
    Huddle01Core.Instance.SendData(data);
}

```

### SendData(string data,List&lt;string&gt; peerIds)

Call this function to send any string data or message selected peers in the room. 

**Parameters**

<OptionTable
  headers={["Params", "Type", "Description"]}
  options={[
    ["data", "string", "Any data or message in form of string"],
    ["peerIds", "string", "List of peerIds"]
    ]}
/>

```csharp filename=".csharp"
public void SendDataToSelectedPeersInRoom(string data,List<string> peerIds)
{
    Huddle01Core.Instance.SendData(data,peerIds);
}

```


### HuddleCore.OnMessageReceived

Subscribe to this event to receive message send by any peer in the room.

**Parameters**

<OptionTable
  headers={["Params", "Type", "Description"]}
  options={[
    ["message", "string", "message in form of json which can be Deserialized into `MessageReceivedResponse`"]
    ]}
/>

```csharp filename=".csharp"

private void OnEnable()
{
    Huddle01Core.OnMessageReceived += OnMessageReceived;
}    

private void OnDisable()
{
    Huddle01Core.OnMessageReceived -= OnMessageReceived;
}

private void OnMessageReceived(string data)
{
    Debug.Log($"received data : {data}");
    MessageReceivedResponse response = JsonConvert.DeserializeObject<MessageReceivedResponse>(data);
}


[System.Serializable]
public class MessageReceivedResponse 
{
    [JsonProperty("from")]
    public string From;

    [JsonProperty("label")]
    public string Label;

    [JsonProperty("payload")]
    public string Payload;
}

```