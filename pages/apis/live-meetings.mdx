---
title: 'Live Meetings API'
---

import JoinRoomToken from '@components/ApiRef/JoinRoomToken';
import JoinRoomTokenResponse from '@components/ApiRef/JoinRoomTokenResponse';
import RequestStrip from '@components/ApiRequest/RequestStrip';
import {
  APIRequestCard,
  APIRequestContent,
  APIResponseCard,
} from '@components/common/ApiCard/ApiCard';
import { Button } from '@components/common/Button/Button';
import HighLight from '@components/common/HighLight';
import Paragraph from '@components/common/Paragraph';
import Pill from '@components/common/Pill/Pill';
import { Callout } from 'nextra-theme-docs';

## **Live Meetings API**

The Live Meetings API provides the ability to fetch live rooms in which people are currently present.
In order to use the API, you will need to obtain an API key by connecting to your wallet.

## **Endpoint**
<div className="flex justify-between lg:flex-row  flex-col w-full">
  <HighLight className="my-3 lg:w-3/4 w-full">
    <RequestStrip
      method={'GET'}
      url={'https://api.huddle01.com/api/v1'}
      endpoint={'live-meetings'}
    />
  </HighLight>
  <Button
    variant="primary"
    url="/api-keys"
    text="Get your API Key"
    isRouting
    className=" my-2 w-auto px-8"
  />
</div>


<Paragraph className="mt-5">
 Whatever meetings are created using Create Room API and are joined by users, will be listed in the Live Meetings API. The API will return the list of all the rooms which are currently live and the number of users present in each room.


</Paragraph>

<div className="grid md:grid-cols-2 gap-4 grid-cols-1 mt-10  md:h-[24.5rem]">
<div>
<div className="flex items-center gap-4">
<Pill >GET</Pill>
<div className="text-sm font-normal text-slate-50">`/live-meetings`</div>

</div>

<div className="mt-7 font-mono text-2xl font-bold text-[#f1f5f9]">
  Get All Live Meetings API
</div>

The API supports a **GET** request with the following required parameters:

- **`x-api-key`** - Add your API Key to the headers to fetch all rooms associated with your API Key.


</div>

<APIRequestCard
    requestType = "GET"
    route="/live-meetings"
    
  >

<APIRequestContent value="axios">
      ```js
      const response = await axios.get(
        'https://api.huddle01.com/api/v1/live-meeting',
        {},
        {
          headers: {
            'Content-Type': 'application/json',
            'x-api-key': {{API_KEY}},
          },
        }
    );
      ```
    </APIRequestContent>

    
    <APIRequestContent value="fetch">
      ```js
     const  response = await fetch("https://api.huddle01.com/api/v1/live-meeting", {
     
    method: "GET",
    {},
    headers: {
        "Content-type": "application/json",
        'x-api-key': {{API_KEY}},
    }
})
      ```
    </APIRequestContent>

    <APIRequestContent value="cUrl">
      ```bash
curl -X POST \
  'https://api.huddle01.com/api/v1/live-meetings' \
  --header 'Accept: */*' \
  --header 'x-api-key: {{API_KEY}}' \
  --header 'Content-Type: application/json' \
  
      ```
    </APIRequestContent>
  </APIRequestCard>

</div>

<div className="grid md:grid-cols-2 gap-4 grid-cols-1 mt-10">
<div className="text-start ">

<div className="font-mono text-2xl font-bold text-[#f1f5f9]">Response</div>

Upon success, the API will return a response with an array of rooms with the following fields:

- **`title`** - The title of the room passed during room creation.
- **`hostWalletAddress`** - An array of wallet addresses who are assigned as hosts.
- **`roomId`** - The roomId of the room created using Create Room API.


In case of an error, the API will return an error message with a corresponding status code.

</div>

<APIResponseCard>
  <APIRequestContent value="Success">
    ```jsx
      {
          {
            "liveMeetings": [
              {
                "title": "first meeting",
                "hostWalletAddress": [],
                "roomId": "wpd-hjnj-cyf"
              },
              {
                "title": "second meeting",
                "hostWalletAddress": [],
                "roomId": "gvh-pmpn-nbe"
              }
            ]
          }
      }
    ```
  </APIRequestContent>

  <APIRequestContent value="Error">

    ```jsx
    {
        "message": "Input validation failed",
        "code": "BAD_REQUEST",
        "issues": [
            {
                "code": "invalid_type",
                "expected": "string",
                "received": "number",
                "path": [
                    "roomId"
                ],
                "message": "Expected string, received number"
            }
        ]
    }
    ```

  </APIRequestContent>
</APIResponseCard>

</div>


## **Endpoint**
<div className="flex justify-between">
  <HighLight className="my-3 w-3/4">
    <RequestStrip
      method={'GET'}
      url={'https://api.huddle01.com/api/v1'}
      endpoint={'live-meeting/?roomId=YOUR_ROOM_ID'}
    />
  </HighLight>
  <Button
    variant="primary"
    url="/api-keys"
    text="Get your API Key"
    isRouting
    className=" my-2 w-auto px-8"
  />
</div>

<Callout>
  If the room isn't live, the API will throw a 404 NOT FOUND error.
</Callout>

<Paragraph className="mt-5">
 Whatever meetings are created using Create Room API and are joined by users, will be listed in the Live Meetings API. The API will return the list of all the rooms which are currently live and the number of users present in each room.


</Paragraph>

<div className="grid md:grid-cols-2 gap-4 grid-cols-1 mt-10  md:h-[24.5rem]">
<div>
<div className="flex items-center gap-4">
<Pill >GET</Pill>
<div className="text-sm font-normal text-slate-50">`/live-meeting`</div>

</div>

<div className="mt-7 font-mono text-2xl font-bold text-[#f1f5f9]">
  Get Single Live Meeting API
</div>

The API supports a **GET** request with the following required parameters:

- **`x-api-key`** - Add your API Key to the headers to fetch all rooms associated with your API Key.
- **`roomId`** - Add the roomId in query params to fetch the details of a single active room.


</div>

<APIRequestCard
    requestType = "GET"
    route="/live-meeting"
    
  >

<APIRequestContent value="axios">
      ```js
      const response = await axios.get(
        'https://api.huddle01.com/api/v1/live-meetings',
        {
          roomId: 'YOUR_ROOM_ID',
        },
        {
          headers: {
            'Content-Type': 'application/json',
            'x-api-key': {{API_KEY}},
          },
        }
    );
      ```
    </APIRequestContent>

    
    <APIRequestContent value="fetch">
      ```js
     const  response = await fetch("https://api.huddle01.com/api/v1/live-meetings", {
     
    method: "GET",
    {
      roomId: 'YOUR_ROOM_ID',
    },
    headers: {
        "Content-type": "application/json",
        'x-api-key': {{API_KEY}},
    }
})
      ```
    </APIRequestContent>

    <APIRequestContent value="cUrl">
      ```bash
curl -X GET \
  'https://api.huddle01.com/api/v1/live-meetings' \
  --header 'Accept: */*' \
  --header 'x-api-key: {{API_KEY}}' \
  --header 'Content-Type: application/json' \
  --data-raw '{
    "roomId" : "YOUR_ROOM_ID",
    }'
      ```
    </APIRequestContent>
  </APIRequestCard>

</div>

<div className="grid md:grid-cols-2 gap-4 grid-cols-1 mt-10">
<div className="text-start ">

<div className="font-mono text-2xl font-bold text-[#f1f5f9]">Response</div>

Upon success, the API will return a response with the following fields:

- **`title`** - The title of the room passed during room creation.
- **`hostWalletAddress`** - An array of wallet addresses who are assigned as hosts.
- **`roomId`** - The roomId of the room created using Create Room API.



</div>

<APIResponseCard>
  <APIRequestContent value="Success">
    ```jsx
      {
        {
          "title": "first meeting",
          "hostWalletAddress": [],
          "roomId": "wpd-hjnj-cyf"
        },
      }
    ```
  </APIRequestContent>

  <APIRequestContent value="Error">

    ```jsx
    {
        "message": "Input validation failed",
        "code": "BAD_REQUEST",
        "issues": [
            {
                "code": "invalid_type",
                "expected": "string",
                "received": "number",
                "path": [
                    "roomId"
                ],
                "message": "Expected string, received number"
            }
        ]
    }
    ```

  </APIRequestContent>
</APIResponseCard>

</div>

